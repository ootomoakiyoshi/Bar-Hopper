<div class="container">
  <div class="row no-gutters">
    <div class="col-md-5 col-sm-10 mx-2 mx-sm-auto shadow-lg rounded px-0 my-5">
      <h5 class="text-dark bg-light p-4 mb-0">Bar Post</h5>
      <div class="form-body p-4">
        <% if @bar.errors.any? %>
          <%= @bar.errors.count %>件のエラーが発生しました
          <% @bar.errors.full_messages.each do |message| %>
            <%= message %>
          <% end %>
        <% end %>
        <%= form_with model: @bar, local:true do |f| %>
          <%= f.attachment_field :image, class: "w-100" %>
          <div class="input-group mb-3 mt-4">
            <div class="input-group-prepend">
              <span class="input-group-text" aria-hidden="true"><i class="fas fa-building"></i></span>
            </div>
            <%= f.text_field :bar_name, class:"form-control", placeholder:"Bar Name" %>
          </div>
          <%= f.label :adress, '住所' %>
          <%= f.text_field :address %>
          <%= f.text_area :caption, class:"form-control", rows:"3", placeholder:"説明" %>
          <hr>
          <%= f.submit '投稿', class:"btn btn-lg btn-block btn-secondary mt-4" %>
        <% end %>
      </div>
    </div>
  </div>
</div>

