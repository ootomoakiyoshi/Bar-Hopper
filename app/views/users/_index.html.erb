<table class='table'>
  <thead>
    <tr>
      <th>image</th>
      <th>name</th>
      <th colspan="3"></th>
    </tr>
  </thead>
   <% @users.each do |user| %>
      <tr>
        <tb><%= attachment_image_tag @user, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg" %></tb>
        <td><%= user.name %></td>
        <td><%= user.introduction %></td>
       <!--フォロー機能-->

       <!--フォロー機能-->
        <td><%= link_to 'Show', user_path(user), class: "user_#{user.id}" %></td>
      </tr>
    <% end %>
  <tbody>
  </tbody>
</table>